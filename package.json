{
  "dependencies": {
    "@actions/core": "^1.7.0",
    "@actions/github": "^5.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.14.38",
    "typescript": "^4.6.4"
  },
  "scripts": {
    "build": "esbuild index.ts --outfile=dist/index.js --bundle --platform=node --loader:.ts=ts"
  }
}